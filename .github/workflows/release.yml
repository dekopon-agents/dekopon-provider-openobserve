name: Release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  release:
    uses: dekopon-agents/provider-workflows/.github/workflows/release.yml@b18553fe324fba779cb8d6383b6eb27f06850272 # v2
    permissions:
      contents: write
      id-token: write
      attestations: write
      packages: write
